import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DPS 926 App 3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
